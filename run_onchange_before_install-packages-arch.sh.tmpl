{{- $isArch := false -}}
{{- if eq .chezmoi.os "linux" -}}
{{-   $isArch = or (eq .chezmoi.osRelease.id "arch") (contains "arch" (index .chezmoi.osRelease "idLike" | default "")) -}}
{{- end -}}
{{- if $isArch -}}
#!/bin/bash
set -euo pipefail

sudo pacman -Sy --needed --noconfirm base-devel curl file git

{{ template "brew-install.sh" . }}
{{- end -}}
